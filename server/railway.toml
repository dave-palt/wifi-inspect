[build]
builder = "bun"
buildCommand = "bun install --frozen-lockfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/v1/health"
port = 3000
interval = 30
timeout = 10

[vars]
PORT = "3000"
NODE_ENV = "production"
